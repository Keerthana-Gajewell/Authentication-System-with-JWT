# Authentication System with JWT

Simple authentication system using Node.js, Express and MongoDB.
📌 API Endpoints
+ Register User
POST /api/auth/register
+  Login User
POST /api/auth/login
login
+  Get Profile (Protected)
Header: Authorization: Bearer <token>

🧪 Postman Collection
- Contains:
  - Register
  - Login
  - Protected Route Test
  - Token Expiry Test
